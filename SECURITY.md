# 安全策略

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| `0.1.x` | 维护中 |

## 报告问题

不要在公开 Issue 中粘贴 API Key、Cookie、数据库或带个人信息的原始视频数据。请通过 GitHub 私信联系维护者，说明复现步骤、影响范围和不包含敏感数据的日志片段。

## 使用注意

- `config.json`、`DOUYIN_COOKIE` 和 API Key 只放在本机或部署平台的密钥配置中。
- 公网部署必须设置 `APP_PASSWORD`，并使用 HTTPS 和防火墙限制访问。
- 只处理允许访问的公开内容，遵守来源网站的服务条款、robots 规则和版权要求。
- 不要把应用配置成任意 URL 抓取代理，也不要启用绕过验证码或反爬技术的方案。
