services:
  - type: web
    name: cook-assistant
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: CLOUD_MODE
        value: "1"
      - key: APP_PASSWORD
        sync: false
